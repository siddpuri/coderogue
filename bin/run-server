#!/usr/bin/bash

cd $(dirname "$0")/..
while true; do
    git pull
    npm run serve
    (
        cd ~/players
        mysqldump -u game -pgame game > dump.sql
        git add .
        git commit -m 'Daily update'
        git push
    )
done

